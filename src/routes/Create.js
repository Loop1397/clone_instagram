import Header from "../Header";

function Create() {
    return (
        <section>
            <header>
                <Header />
            </header>
        </section>
    );
}

export default Create;
