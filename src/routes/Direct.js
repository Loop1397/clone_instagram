import Header from "../Header";

function Direct() {
    return (
        <section>
            <header>
                <Header />
            </header>
        </section>
    );
}

export default Direct;
